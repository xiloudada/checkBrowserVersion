/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: "export",
    // reactStrictMode: true,
    // compress: true,
    // basePath:'/',
    // assetPrefix:'/'
    // publicRuntimeConfig:'/'
}

module.exports = nextConfig
