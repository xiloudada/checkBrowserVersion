/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    distDir: 'build',
    reactStrictMode: true,
    compress: true,
    // basePath:'/',
    // assetPrefix:'/'
    // publicRuntimeConfig:'/'
}

module.exports = nextConfig
